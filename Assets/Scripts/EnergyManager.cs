using UnityEngine;
// 에너지 관리
public class EnergyManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
    
    public void Init()
    {
        Debug.Log("EnergyManager initialize");

    }
}
